"""e2e smoke test"""

load("@monogres//postgres:cfg.bzl", "CFG")
load(":test.bzl", "build_smoke_test")

# Exercise build_all_test with just the default target (one PG version + option
# set) instead of every combination.
build_smoke_test(
    name = CFG.name,
    cfg = CFG,
)
