"""e2e smoke test"""

load("@monogres//extensions:all.bzl", "CFGS_ALL")
load(":test.bzl", "build_smoke_test")

# Loading CFGS_ALL validates every generated cfg.bzl / repo.bzl. Exercise
# build_all_test with just citus's default target: it covers deps_buildtime,
# deps_runtime, and is_compatible filtering.
build_smoke_test(
    name = "citus",
    cfg = CFGS_ALL["citus"][0],
)
