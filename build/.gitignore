# Ignore all bazel-* symlinks. Globbing because the workspace name can change
# depending on the name of the directory bazel is cloned into.
bazel-*

# ignoring MODULE.bazel.lock for the time being
# see https://github.com/bazelbuild/bazel/issues/20369
MODULE.bazel.lock

# ignore the user's Bazel config
.bazelrc.user

# ignore the "Makefile locks"
/docker/.*.lock

# Temp ignore: ondb-cloud clone
/ondb-cloud
